{
  "name": "openid-connect-demo",
  "version": "1.0.0",
  "description": "OpenID Connect demo with oidc-provider using npm workspaces",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start:provider": "npm run start --workspace=@demo/provider",
    "start:admin-backend": "npm run start --workspace=@demo/admin-backend",
    "build:admin-ui": "npm run build --workspace=@demo/admin-ui",
    "start:all": "npm run build:admin-ui && concurrently \"npm run start:provider\" \"npm run start:admin-backend\"",
    "dev:provider": "npm run dev --workspace=@demo/provider",
    "dev:admin-backend": "npm run dev --workspace=@demo/admin-backend",
    "dev:admin-ui": "npm run dev --workspace=@demo/admin-ui",
    "dev:all": "concurrently \"npm run dev:provider\" \"npm run dev:admin-backend\" \"npm run dev:admin-ui\"",
    "install:all": "npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": [
    "openid-connect",
    "oidc",
    "oidc-provider",
    "demo"
  ],
  "author": "",
  "license": "MIT"
}
